<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.tedu.csmall.server.mapper.AlbumMapper">

    <!-- int insert(Album album); -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into pms_album (
            name, description,sort,gmt_create, gmt_modified
        ) values (
            #{name}, #{description},#{sort},#{gmtCreate}, #{gmtCodified}
                 )
    </insert>


    <!--int deleteById(Long id);-->
    <delete id="deleteById">
        delete from pms_album where id=#{id}
    </delete>

    <update id="updateNameById">
        update pms_album set name= #{name} where id=#{id}
    </update>

    <!-- int deleteByIds(Long... ids); -->
    <delete id="deleteByIds">
        delete from pms_album where id in (
        <foreach collection="array" item="id" separator=",">
            #{id}
        </foreach>
        )
    </delete>


    <select id="count" resultType="int">
        select count(*) from pms_album
    </select>

    <select id="getById" resultType="cn.tedu.csmall.server.pojo.vo.AlbumDetailVO">
        select
        <include refid="DetailQueryFields" />
        from
        pms_album

        where
        id=#{id}
    </select>

    <sql id="DetailQueryFields">
        <if test="true">
            id,name,description,sort
        </if>
    </sql>

    <select id="list" resultType="cn.tedu.csmall.server.pojo.vo.AlbumListItemVO">
        select
        <include refid="ListItemQueryFields" />
        from
        pms_album
        order by id
    </select>

    <sql id="ListItemQueryFields">
        id,name,description,sort
    </sql>


</mapper>
